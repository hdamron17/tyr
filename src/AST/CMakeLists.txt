### Copyright 2019 Hunter Damron

subdir_include()

# set(AST_LIB ${PROJECT_NAME}_ast)
# list(APPEND PROJECT_LIBS ${AST_LIB})
# set(PROJECT_LIBS "${PROJECT_LIBS}" PARENT_SCOPE)
# add_library(${AST_LIB} OBJECT )
